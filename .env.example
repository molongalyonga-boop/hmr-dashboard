# Vercel KV auto-injects these when you add a KV store to the project.
# (Storage -> Create -> KV -> connect to this project. No manual paste needed if connected.)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Google sign-in (same gate as the tracker form)
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
ALLOWED_DOMAIN=hmr-consultinggroup.com

# The Apps Script web app /exec URL that returns the responses as JSON.
# Deploy HMR-Dashboard-Data.gs as a web app (Execute as: Me, Access: Anyone) and paste its URL here.
DATA_URL=https://script.google.com/macros/s/AKfycb.../exec
